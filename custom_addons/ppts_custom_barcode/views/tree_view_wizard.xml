<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<data>
        <record id="custom_mobile_view_tree_form" model="ir.ui.view">
            <field name="name">custom.mobile.view.tree.form</field>
            <field name="model">custom.mobile.view.tree</field>
            <field name="arch" type="xml">
                <form create="0" edit="1" delete="0">
                    
                    <header>
                    </header>
                    
                    <field name="is_stock_picking" invisible="1"/>
                    <field name="is_project_container" invisible="1"/>
                    
                    <field name="stock_picking_ids" attrs="{'invisible': [('is_stock_picking', '=',False)]}">
                        <tree create="0" delete="0">
                            <field name="name"/>
                            <field name="location_id" />
                            <field name="location_dest_id" string="To" groups="stock.group_stock_multi_locations" optional="show"/>
                            <field name="partner_id" optional="show"/>
                            <field name="vendor_ref"/>
                            <field name="user_id" optional="hide"/>
                            <field name="date" invisible="1"/>
                            <field name="scheduled_date" optional="show"/>
                            <field name="origin" optional="show"/>
                            <field name="group_id" invisible="1"/>
                            <field name="backorder_id" optional="hide"/>
                            <field name="state" optional="show"/>
                            <field name="priority" optional="hide"/>
                            <field name="picking_type_id" optional="hide"/>
                            <field name="company_id" groups="base.group_multi_company" optional="show"/>
                            <field name="activity_exception_decoration" widget="activity_exception"/>
                        </tree>
                    </field>
                    
                    <field name="project_container_ids" attrs="{'invisible': [('is_project_container', '=',False)]}">
                        <tree create="0" delete="0">
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="picking_id"/>
                            <field name="partner_id"/>
                            <field name="container_type_id"/>
                            <field name="main_product_id"/>
                            <field name="sub_product_id"/>
                            <field name="gross_weight" sum="Gross Weight"/>
                            <field name="net_weight" sum="Fraction Net Weight"/>
                            <field name="volume" sum="Volume"/>
                            <field name="supervisor_id"/>
                            <field name="location_id"/>
                            <field name="action_date"/>
                            <field name="state"/>
                            
                        </tree>
                    </field>

<!--                     <footer>
                        <button string="Close" class="btn btn-sm btn-default" special="cancel"/>
                    </footer> -->
                </form>
            </field>
        </record>
    </data>
</odoo>